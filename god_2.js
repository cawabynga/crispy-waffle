// фун.-я целочисленного деления
var div = function (val, by) {
    return (val - val % by) / by;
};
var winning = function (conditions) {
    var stroke = [0, 0, 0];
    var string = [0, 0, 0];
    var diagonals = [0, 0];
    for (var i1 = 0; i1 < 3; i1++) {
        var current_stroke = [0, 0, 0];
        var current_string = [0, 0, 0];
        for (var i2 = 0; i2 < 3;  i2++) {
            
        }
    }
};