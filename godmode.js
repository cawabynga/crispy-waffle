var winning = function (cond, diff) {
    if (diff === 'god') {
        if (turn === 9 && win(cond) === 2) {
            return 'YES'
        } else {
            zero = 10;
            while (zero !== -1) {

            }
        }
    }
};
